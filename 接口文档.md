## 注册


:::tips
+ **URL**：` /api/v1/auth/register  `
+ **Method**：POST
+ **需要登录**：<font style="background:#F6E1AC;color:#664900">否</font>
+ **需要鉴权**：<font style="background:#F6E1AC;color:#664900">否</font>

:::

### 请求参数
| 参数 | 类型 | 是否必填 | 约束 |
| --- | --- | --- | --- |
| user_name | String | 是 | 1 到 30 个字符 |
| password | String | 是 | 1 到 30 个字符 |
| confirm_password   | String | 是 | 1 到 30 个字符 |
| phone | String | 否 | 11个字符 |
| email | String | 否 | 1 到 30 个字符   含“@”和“com” |
|  |  |  |  |


:::info
💡 注意，id 和 email 字段目前是只读属性，不允许通过此接口进行修改。

:::

### 请求示例
可以仅传递部分请求参数。

```json
{
    "user_name": "John",
    "password":	"123456",
    "confirm_password":	"123456"
}
```

可以通过传递空字符串来清除数据。

```json
{
    "user_name": ""
}
```

### 成功响应
:::tips
**条件**：请求参数合法，并且参数校验通过。

**状态码：**`200 OK`

**响应示例**：响应会将注册成功状态返回

:::

```json
{
    code：2000
    msg：Sucesse
    data：[]
}
```

### 错误响应
:::tips
**条件**：请求数据校验未通过，例如 user_name 的已存在。

**状态码**：`400 BAD REQUEST`

响应示例:

:::

```json
{
    code：4000
    msg：UserName already exist.
    data：[]
}
```

### 注意事项
:::info
💡 如果用户的用户信息不存在，将会使用请求的数据创建一个新的用户信息。

:::





